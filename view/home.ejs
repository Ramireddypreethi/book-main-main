<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fixed Navbar with Background Video</title>
    <style>
        /* Style the navigation bar */
        .navbar {
            overflow: hidden;
            background-color: lightslategray; /* Add background color with transparency */
            position: fixed;
            top: 0;
            width: 100%;
            padding-bottom: 20px;
        }

        /* Style the navigation bar links */
        .navbar a {
            float: left;
            display: block;
            color: white;
            text-align: center;
            font-size: 25px;
            padding: 14px 16px;
            text-decoration: none;
        }

        /* Align "Register" and "Login" links to the right */
        .navbar .right-links {
            float: right;
        }

        /* On hover, the links will change color */
        .navbar a:hover {
            background-color: #ddd;
            color: white;
        }

        /* Style the video container */
        .video-container {
            position: relative;
            width: 100%;
            height: 100vh;
            margin-top: 60px;
        }

        /* Style the video element */
        video {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* Style the content */
        .content {
            position: relative;
            z-index: 1;
            text-align: center;
            padding: 20px;
            color: white;
            margin-top: 60px;
        }

        /* Style the picture */
        .centered-picture {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        /* Style the "Get Started" button */
        .get-started-button {
            background-color: #007BFF;
            color: white;
            padding: 10px 20px;
            font-size: 20px;
            text-decoration: none;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        /* Style the "Get Started" button on hover */
        .get-started-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

<div class="navbar">
    <a href="#">Book Store</a>

    <div class="right-links">
        <a href="/register">Register</a>
        <a href="/login">Login</a>
    </div>
</div>

<div class="video-container">
    <video autoplay loop muted>
        <source src="../static/books.mp4" type="video/mp4">
        Your browser does not support the video tag.
    </video>
    <!-- Add a centered picture -->

    

    <!-- Add "Get Started" button -->
    <a class="centered-picture get-started-button" href="/register">Get Started</a>
</div>

<!-- Your content goes here -->
<div class="content">
    <h1>Welcome to Our Website</h1>
    <p>This is some content on the page.</p>
</div>

</body>
</html>
